/*!
 * Kathat Estate — frontend configuration.
 * This is the ONLY thing you need to edit before deploying this site.
 * Point it at wherever the backend (kathat-backend/) ends up running.
 *
 * Examples:
 *   window.KATHAT_API_BASE = "https://api.kathatestate.com";
 *   window.KATHAT_API_BASE = "http://localhost:8000";   // local backend testing
 *   window.KATHAT_API_BASE = "";                          // same-origin (behind a reverse proxy)
 */
window.KATHAT_API_BASE = "http://localhost:8000";
